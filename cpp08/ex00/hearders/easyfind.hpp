#ifndef EASYFIND_HPP
#define EASYFIND_HPP

template<typename T>
T& easyfind(T a, int b)
{
try
{
    for(int i = a.begin(); a.begin() != a.end(); a.begin++) 
    {
        if(a = b)
        return *T;
    }
    throw notFound;
} catch(std::exception &e)
{
std::cout << "exception caught: " << e.what() << std::endl;
}
    
return *T
}